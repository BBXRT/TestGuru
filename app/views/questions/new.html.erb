<h1>Новый вопрос (для "<%= test_title %>"):</h1>

<form action="/tests/<%= params[:test_id] %>/questions/<%= params[:id] %>" method="post">
  <input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
  <input type="text" name="question[body]" id="question-body">
  <input type="submit" value="Создать">
</form>

********

<%= form_with(url: "/tests/#{params[:test_id]}/questions/#{params[:id]}", method: "post") do |form| %>
  <%= form.text_field :body %> #этот инпут не работает'
  <input type="text" name="question[body]" id="question-body"> # а этот работает, но не показывает сообщение о созданном воропсе
  <%= form.submit %>
<% end %>
